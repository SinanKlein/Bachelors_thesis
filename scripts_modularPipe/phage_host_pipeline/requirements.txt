# pip install -r requirements.txt   (exact versions of a past run: requirements.lock.txt)
numpy>=2.2,<3
pandas>=2.3,<3
scipy>=1.15,<2
scikit-learn>=1.7,<2
PyYAML>=6.0
joblib>=1.5
matplotlib>=3.10
xgboost>=2.0
torch>=2.0

# R plots (plots/*.R), from an R prompt:
#   install.packages(c("ggplot2", "dplyr", "tidyr", "readr", "tibble", "jsonlite", "scales", "patchwork"))
